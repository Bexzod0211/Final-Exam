<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_home"
        android:title="Home"
        android:icon="@drawable/ic_home"
        app:showAsAction="ifRoom"
        />

    <item android:id="@+id/menu_appointment"
        android:title="Appointment"
        android:icon="@drawable/ic_appointment"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/menu_history"
        android:title="History"
        android:icon="@drawable/ic_history"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/menu_articles"
        android:title="Articles"
        android:icon="@drawable/ic_article"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/menu_profile"
        android:title="Profile"
        android:icon="@drawable/ic_profile"
        app:showAsAction="ifRoom"/>
</menu>